
public class Trie { //prefix trie for storing dictionary
	Trie[] subtries;
	boolean endofWord;
	
	public Trie() {
		subtries = new Trie[26]; //26 letters
		endofWord = false;
	}
	
	public void insert(String s) { //inserts String into root trie;
		Trie root;
		for(int x = 0; x < s.length(); x++) {
			root = subtries[s.charAt(x)- 'A'];
			
			
		}
			
			
		
		
	}
	
}
